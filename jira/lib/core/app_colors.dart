import 'package:flutter/material.dart';

class AppColors {
  // Màu xanh mới thay cho tím cũ
  static const Color primary = Color(0xFF3366FF);
  static const Color primaryDark = Color(0xFF274FCB);
  static const Color accent = Color(0xFF3366FF);
}
