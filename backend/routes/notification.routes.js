// import express from 'express';
// import { commentTask, assignTask, addMember } from "../controllers/notify.controller.js";
// import { verifyToken } from '../middleware/authMiddleware.js';

// const route = express.Router();

// route.post("/comment", verifyToken, commentTask);
// route.post("/assign", verifyToken, assignTask);
// route.post("/add-member", verifyToken, addMember);

// export default route;
